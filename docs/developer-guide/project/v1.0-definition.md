# Kaiku v1.0 — Definition of Done

> Synthesized 2026-06-12 from the goal-driven hardening run (PRs #571–#592)
> and `goals.md` Goal 7. This is the concrete gate for tagging `v1.0.0`:
> what must be true, what's already true, and the product decisions that
> only the maintainer can make.

## Status at a glance

| Gate | State |
|---|---|
| Green CI; dependency sweep closed | ✅ done (Goal 1) |
| Desktop client feature-complete (Phase 6) | ✅ done (Goal 2) |
| Docs reflect verified code state | ✅ done (Goal 3) |
| Beta ops: backups, restore runbook, reproducible infra | ✅ repo-side (Goal 4) — *operator drill pending* |
| Reliability gates: isolation, perf budgets, upgrade safety, diagnostics | ✅ done (Goal 5) |
| Accessibility baseline + admin command center | ✅ done (Goal 6 items 1–2) |
| Self-hosted sovereignty (STUN/TURN) | ✅ done |
| Android public beta with feature matrix | ◻ milestone defined, beta not shipped |
| Windows desktop: fixed or formally dropped | ✅ green in CI (TD-25 resolved) — **keep**, not drop |
| Product decisions: i18n, iOS, SaaS/billing | ⛔ **maintainer input required** |
| `v1.0.0` release: signed artifacts + upgrade playbook + public changelog | ◻ blocked on the above |

## What is already true (no further work)

- **CI is green** across all workflows; the Security Audit no longer fails;
  every advisory exception is dated and justified.
- **Desktop client** has connection metrics, output-device selection,
  reconnect-safe unreads, a system tray, and **signed auto-update** via
  GitHub Releases.
- **Reliability is enforced in CI**, not just documented: tenancy/isolation
  regression tests, bundle-size + server-latency budgets, and a server-side
  diagnostics endpoint.
- **Operators have runbooks**: backup/restore, upgrade playbook with a
  preflight script, and an ops index — and a fresh VPS can be rebuilt from
  the repo (the previously-missing RustFS compose service was added).
- **Voice is sovereign by default** on self-hosted nodes (STUN points at the
  operator's coturn, not Google).

## Remaining engineering before v1.0

These are buildable without a product decision:

1. **Operator backup drill** (Goal 4 tail) — one documented restore drill on
   the live VPS. Operator-run; ~20 min. (`docs/ops/backup-restore-runbook.md`)
2. **Android beta** — ship the 5 blocker features (markdown, attachment
   display, unread indicators, DM UI, reaction picker) from the milestone in
   `roadmap.md`, add an instrumented UI smoke suite, one week of daily use.
3. **Release dry-run** — tag a `v1.0.0-rc`, confirm the signed desktop
   artifacts and `latest.json` updater manifest publish, and that an older
   build offers the update. (First real exercise of the auto-update pipeline.)
4. **Back up the Tauri updater signing key** (`~/.tauri/kaiku.key`) — losing
   it permanently breaks auto-update for installed clients.

## Product decisions blocking v1.0 (maintainer only)

These cannot be resolved by engineering judgment — they set scope:

- **i18n**: the client is English-only with no i18n framework. Decide:
  ship v1.0 English-only (document as a known limitation), or make i18n a
  v1.0 gate (a substantial addition).
- **iOS**: no iOS client exists. Decide: Android-only for v1.0, or hold v1.0
  for iOS parity.
- **SaaS / billing**: the Stripe/billing track (Goal 6 item 5) is unstarted.
  Decide: v1.0 is self-hosted-only (recommended — matches the project's
  sovereignty thesis), with SaaS as a post-1.0 track.

**Recommendation:** scope v1.0 as **self-hosted, desktop + Android beta,
English-only**, with i18n / iOS / SaaS explicitly deferred to a documented
post-1.0 roadmap. That ships the sovereignty-first product the codebase is
already built for, without gating on speculative SaaS work.

## Release checklist (when the gate is met)

- [ ] Android beta criteria met (above)
- [ ] Operator restore drill performed and logged
- [ ] `v1.0.0-rc` dry-run validates signed artifacts + updater manifest
- [ ] Product decisions recorded in this doc
- [ ] `CHANGELOG.md` `[Unreleased]` → `[1.0.0]` with a curated highlight list
- [ ] Tag `v1.0.0`; verify the Release workflow publishes signed desktop
      builds, Docker image, and `latest.json`
- [ ] Updater key backed up off-host
